#pragma once
#include <glad/glad.h>
#include <GLFW/glfw3.h>

enum class InputKey {
    KeyEscape = GLFW_KEY_ESCAPE,
    KeyA = GLFW_KEY_A,
    KeyD = GLFW_KEY_D,
    KeyS = GLFW_KEY_S,
    KeyW = GLFW_KEY_W
};